

import { exportarListaAExcel } from './utils/excel';

// ... dentro de tu componente App ...

const manejarExportacion = () => {
  // Preparamos los datos exactamente como los quiere el bodeguero en Excel
  const datosExcel = listaOrdenada.map(p => ({
    "Producto": p.nombre,
    "Stock": p.stock,
    "Precio ($)": p.precioVentaUSD,
    "Precio (Bs)": p.precioVentaVES
  }));

  exportarListaAExcel(datosExcel, "Mi Bodega");
};

// En tu JSX:
<button 
  onClick={manejarExportacion}
  className="bg-green-700 text-white px-4 py-2 rounded-lg font-bold hover:bg-green-800 transition no-print"
>
  📊 Descargar Excel
</button>
